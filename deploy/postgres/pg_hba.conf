# Socket access is confined to the database container for entrypoint/bootstrap.
local   all   postgres                  trust
local   all   all                       reject
# All network clients, including migrations, must use TLS and SCRAM.
hostnossl all  all       all             reject
hostssl   all  all       all             scram-sha-256
